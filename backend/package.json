{
  "name": "thestudio-backend",
  "version": "2.0.0",
  "description": "The Studio Reno - Yoga Studio Management System",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "db:init": "node scripts/db-init.js",
    "db:setup": "psql $DATABASE_URL -f database/schema.sql && psql $DATABASE_URL -f database/retail-schema.sql && psql $DATABASE_URL -f database/cms-schema.sql && psql $DATABASE_URL -f database/coop-schema.sql",
    "db:seed": "psql $DATABASE_URL -f database/seed.sql",
    "db:reset": "psql $DATABASE_URL -f database/schema.sql && psql $DATABASE_URL -f database/retail-schema.sql && psql $DATABASE_URL -f database/cms-schema.sql && psql $DATABASE_URL -f database/coop-schema.sql && psql $DATABASE_URL -f database/seed.sql",
    "db:migrate": "node scripts/migrate.js",
    "db:migrate:coop": "node scripts/migrate.js coop-schema.sql"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "node-cron": "^4.2.1",
    "pg": "^8.11.3",
    "stripe": "^14.10.0",
    "twilio": "^4.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
