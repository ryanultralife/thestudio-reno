{
  "name": "thestudio-backend",
  "version": "2.0.0",
  "description": "The Studio Reno - Yoga Studio Management System",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start:safe": "node src/safe-start.js",
    "dev": "nodemon src/index.js",
    "db:setup": "psql $DATABASE_URL -f database/schema.sql && psql $DATABASE_URL -f database/retail-schema.sql && psql $DATABASE_URL -f database/cms-schema.sql",
    "db:seed": "psql $DATABASE_URL -f database/seed.sql",
    "db:reset": "psql $DATABASE_URL -f database/schema.sql && psql $DATABASE_URL -f database/retail-schema.sql && psql $DATABASE_URL -f database/cms-schema.sql && psql $DATABASE_URL -f database/seed.sql",
    "db:demo": "node database/populate-demo.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.11.3",
    "stripe": "^14.10.0",
    "@sendgrid/mail": "^8.1.0",
    "twilio": "^4.20.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
